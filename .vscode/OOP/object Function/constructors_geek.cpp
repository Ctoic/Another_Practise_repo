#include<iostream>
using namespace std;
// A constructor is a speciall type of member function 
// In C++ a constructor is automatically called when an object is created
// special member function of class whch do not have any return type
// constructors don't have the return type 
// constructors have the same name as the class itself
// Automatically acalled
//must be placed in the publc sections
// if we do not placed a constructor C++ will generete a default without parameters and body

class Computer{
    private:

    public:


};

int main()
{
    Computer dell;
    Computer hp;


    return 0;
}